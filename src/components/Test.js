import React from 'react';
import '../App.css';

const TestComponent = (props) => {
    return <li className = "not-found"><h3>Test Component</h3><p>This is a test to determine if the correct code is being run.</p></li>}

export default TestComponent;